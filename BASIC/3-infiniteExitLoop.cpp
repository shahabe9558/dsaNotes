#include<iostream>
using namespace std;
int main()
{
    while(1)
    {
        switch (1)
        {
        case 1: cout << "Continue" << endl;
            // break;
            exit(0);
        
        default:
            break;
        }
        // exit(1);
    }
}