#include <iostream>
#include<vector>
using namespace std;
int main()
{
    // vector<int> v;
    // v.push_back(1);
    // v.push_back(2);
    // v.push_back(3);
    // v.push_back(4);
    // cout << v.size();

    // vector<vector<int>> v;
    // v.push_back({1,2});
    // v.push_back({2,3,4});
    // v.push_back({5,6,7});

    // cout << "the size of row is " << v.size() << endl;
    // cout << "the size of columnm is " << v[0].size() << endl;
    // cout << "the size of columnm is " << v[1].size() << endl;
    // cout << "the size of columnm is " << v[2].size() << endl;

    // treavesing the vector
    vector<vector<int>> v;
   v.push_back({1,2,3});
   v.push_back({4,5,6});
   v.push_back({7,8,9});
//    for(int i = 0; i<v.size(); i++)
//    {
//     for(int j = 0; j<v[0].size(); j++)
//     {
//         cout << v[i][j] << " ";
//     }
//     cout << endl;
//    }


}